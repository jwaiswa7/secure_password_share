
<div id="secret">
  <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4 max-w-lg">
    <p class="text-success">A secret link only works once and will disapear for ever one accessed or expired. </p>
    <p class="text-gray-700">
      Share the link below with the password used when creating the secret to access the information.
    </p>
    <input type="text" value="<%= new_secret_access_url(secret) %>" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline mt-4">
    <p class="text-success">Expires in: <%= distance_of_time_in_words(secret.created_at, secret.created_at + secret.life_time.minutes) %></p>
  </div>
</div>