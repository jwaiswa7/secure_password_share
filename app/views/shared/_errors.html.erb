<div id='errors'>
  <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4 max-w-lg">
    <ul class="mb-8">
      <% errors.full_messages.each do |msg| %>
        <li class="text-red-500"><%= msg %></li>
      <% end %>
    </ul>
    <%= link_to 'New Secret', root_path, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline' %>
  </div>
</div>